
// to get all expese / filter by site & date  --- PENDING 
export const getExpenses = async (req, res, next) => {
    try {
        // check the old code
    } catch (error) {
        next(error)
    }
}

// to add new expense  --- PENDING 
export const addNewExpense = async (req, res, next) => {
    try {
        // check the old code
    } catch (error) {
        next(error)
    }
}

// save edited expense  --- PENDING 
export const saveUpdatedExpense = async (req, res, next) => {
    try {
        // check the old code
    } catch (error) {
        next(error)
    }
}

// delete expense  --- PENDING 
export const deleteExpense = async (req, res, next) => {
    try {
        // get expense id
        // soft delete it and save
    } catch (error) {
        next(error)
    }
}
