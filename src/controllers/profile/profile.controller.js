

// add new profile on selcted site ---PENDING 
export const addSiteProfile = async(req, res, next) => {
    try {
        // get profile details
        // get user
        // get site
        // save profile with id of site & added user
    } catch (error) {
        next(error)
    }
}

// get profiles of selcted site ---PENDING
export const getProfileBySite = async(req, res, next) => {
    try {
        // get site
        // find profiles with given site id
        // return 
    } catch (error) {
        next(error)
    }
}